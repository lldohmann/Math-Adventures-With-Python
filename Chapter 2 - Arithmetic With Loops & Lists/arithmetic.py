def average(a,b):
    return (a + b) / 2
def mySum(num):
    running_sum = 0
    for i in range(1,num+1):
        running_sum += i
    return running_sum
def average(numList):
    return sum(numList)/len(numList)

intList = [1, 2, 3]

strList = ['hello', 'world']

myList = [24, False, 'foobar', 3.14]
